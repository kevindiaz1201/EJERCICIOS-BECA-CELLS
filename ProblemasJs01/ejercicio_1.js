let clientes =['cliente1', 'cliente2', 'cliente3'];
let empleados=['empleado1', 'empleado2', 'empleado3'];

//Concat añadió al final del primer arreglo el contenido del arreglo empleados que se envió como parámetro
//console.log(clientes.concat(empleados));

//Join une los elementos de clientes y empleados en una solo string con el separador por default ","
//console.log(clientes.join() + empleados.join());

//Push modifica el arreglo original agregando los elementos de el array que se pasa por parámetro
//clientes.push(...empleados);
//console.log(clientes);

//Splice modifica el arreglo original y añade o elimina elementos
//clientes.splice(clientes.length, 0, ...empleados);
//console.log(clientes);

//Por lo tanto la mejor forma de realizar la tarea es CONCAT, pued no modifica el arreglo original

